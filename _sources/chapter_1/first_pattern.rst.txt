.. _first_pattern:

First own Pattern
=================

1. Explain why CSV-reader should be a pattern
2. Create pattern with simple function (file, interval)
3. Implement pattern in Plugin
4. Implement a CVS-Watcher Store with register(), get(), unregister(), run(), stop()??
5. Update plugin
6. Explain app and plugin context for patterns
7. Implement these levels in pattern
8. add signal in thread
9  add receiver + func in plugin
10. send diff with signal
